# Programa que convierte de Celsius a Fahrenheit

cels = input('¿Cuántos grados Celsius?\n')
cels = float(cels)

fahren = (cels * 9 / 5) + 32

print(str(cels) + 'º centígrados equivalen a ' + str(fahren) + 'º fahrenheit\n')
